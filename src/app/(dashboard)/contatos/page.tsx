import { ContactsList } from "@/components/contacts/ContactsList";

export const metadata = {
  title: "Contatos - AutoSales",
  description: "Gerencie todos os seus contatos",
};

export default function ContactsPage() {
  return <ContactsList />;
}
